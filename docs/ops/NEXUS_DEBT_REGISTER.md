# Registro vivo de deuda — PROMueve Nexus

**Última actualización:** 2026-09-25
**Estado:** `LIVE / OPERATIVE`
**Ámbito:** deuda transversal de plataforma, Foundation, tooling, deployment y seams Nexus
**Issue origen inicial:** #389

Este documento es el inventario rápido de deuda **aceptada** de PROMueve Nexus. Resume estado, riesgo, condición de cierre y destino; la evidencia detallada permanece en los issues/PRs enlazados. No es backlog de ideas, no convierte findings informacionales en trabajo aprobado y no autoriza implementación por sí mismo.

La deuda específica de Farmacia continúa en [`FARMACIA_DEBT_REGISTER.md`](./FARMACIA_DEBT_REGISTER.md). Cuando una deuda Nexus pase a una WO concreta, se conserva aquí como `OPEN`, `TRANSFERRED` o `RESOLVED` con su referencia.

## Deuda registrada

| ID | Estado | Timing / destino | Origen / evidencia | Riesgo concreto | Criterio de cierre | WO / PR |
| --- | --- | --- | --- | --- | --- | --- |
| `NEXUS-DEBT-001` | RESOLVED | Cerrada en TRAIN-NEXUS-FOUNDATION-02 | #389 D1; Promotion Review PR #388; cierre #394 | Los hashes de provenance del manifest se calculaban sobre bytes crudos; LF/CRLF podía cambiar el hash del mismo contenido lógico y degradar reproducibilidad entre checkouts. | Hashes de provenance calculados sobre contenido EOL-canonicalizado, con verificación plantada de invariancia LF/CRLF y de detección de drift real; phantom-dirty del blob CRLF de TOOLING_BASELINE eliminado. | #394 / PR final de TRAIN-NEXUS-FOUNDATION-02 |
| `NEXUS-DEBT-002` | RESOLVED | Cerrada en TRAIN-NEXUS-FOUNDATION-02 antes de consumo runtime F3 | #389 D2; Promotion Review PR #388; cierre #395 | Los checkers validaban módulos presentes, pero un artefacto manual podía omitir un módulo esperado y pasar la semántica previa. | Manifest demostrado completo respecto al profile y readiness respecto al manifest (ambas direcciones); negativos plantados `manifest-missing-module` / `readiness-missing-module`. | #395 / PR final de TRAIN-NEXUS-FOUNDATION-02 |
| `NEXUS-DEBT-003` | RESOLVED | Cerrada post Bootstrap 01 | #389 D3 | INDEX/WOS podían quedar stale tras merge de #388. | Estado publicado reconciliado con candidate y merge separados. | #390 / PR #391 → merge `e6309173...` |
| `NEXUS-DEBT-004` | OPEN | F2 hardening / F3 integration cuando el release map sea frontera consumida | #389 D4 | `module-releases.json` no tiene schema propio y `readiness: demonstrated` no transporta evidencia explícita; riesgo de inconsistencia al evolucionar la familia de contratos. | Decidir bajo presión real si necesita schema/evidence pointer y verificarlo; no inventar jerarquía antes de uso. | Pendiente |
| `NEXUS-DEBT-005` | OPEN / parcialmente resuelto | Distribuir por WOs futuras; no cleanup amplio | #389 D5 + reviews de #388 | Cobertura/mantenibilidad menor: baseline tooling desactualizado respecto a `ajv` (subhallazgo resuelto por #394), falta caso booleano `false` en corpus Reuma, comentario de orden incoherente, helper muerto, ROOT inconsistente, newline package, timeout/cuota VM y consola lossy. | Cada subhallazgo restante queda resuelto, transferido a WO específica o descartado con rationale; no cerrar por barrido oportunista. | Parcial: #394; resto Pendiente |
| `NEXUS-DEBT-006` | RESOLVED | Cerrada en F3.1-E antes de F3.2 | Promotion Review v1 PR #400 (nonblocking) | `PlatformContext.fromSnapshot` aceptaba snapshots fabricados que cumplían shape/freeze; el test de aislamiento de input mutaba una copia post-load. | Cerrado por F3.1-E `4ee94c2`: registro de emisión interno no enumerable (WeakSet) + código `SNAPSHOT_UNTRUSTED_ORIGIN` (lookalike fabricado rechazado) y test real de aislamiento que muta el mismo input original tras `load()`. | F3.1-E / PR #400 |
| `NEXUS-DEBT-007` | OPEN | Antes del strangler/export hardening Reuma; no bloquea F3.2 Home | Promotion Review v1 final PR #400 (nonblocking) | Los planted tests N1/G1 de `reuma_export_acceptance_check.mjs` usan un resultado con una sola de las diez jornadas; aunque la fila mutada deje de tener 496 campos, A1 seguiría fallando por las otras nueve jornadas ausentes. El corpus completo sí demuestra longitud 497, pero N1/G1 no aíslan por sí solos la falsificación específica que declaran. | Plantar una falsificación que mantenga las diez jornadas y altere únicamente la longitud objetivo, o ajustar el evaluador de test para que la causa esperada sea inequívoca; demostrar que el negativo deja de pasar si se retira específicamente la detección de 496 campos. | Pendiente |
| `NEXUS-DEBT-008` | RESOLVED | Cerrada en TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409), antes de F3.4 | Promotion Review v1 final PR #400 (nonblocking); cierre #412 | `deployment_manifest_build.mjs` rechaza `moduleId` duplicados pero puede emitir dos módulos con el mismo `entryPath`; `ConfigurationRepository.load` ya rechaza fail-closed ese artefacto, por lo que no existe navegación ambigua aceptada en runtime, pero builder y frontera runtime no están alineados. | `deployment_manifest_build.mjs` rechaza `entryPath` duplicado sobre la composición resuelta antes de emitir, con mensaje determinista; fixture plantado `registry-duplicate-entry-path.json` y negativo del checker 12/0; protección runtime de `ConfigurationRepository.load` intacta. Commit #412 `b32ab74`; `review_due=false`. | #412 (`b32ab74`) / TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409) |
| `NEXUS-DEBT-009` | RESOLVED | Cerrada en TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409), junto con F3.3 | Auditoría post-RDD #403/#404; issue #405; cierre #410 | `package.json` expone `check:home` y `check:home-navigation`, pero `verify:nexus`/CI no los ejecuta; una regresión Home futura podría dejar CI verde si no afecta gates previos. | `check:home` y `check:home-navigation` cableados en `verify:nexus` inmediatamente después de `check:platform`, de modo que CI `nexus-checks.yml` los ejecuta sobre fresh checkout. Commit #410 `74f9963`. | #410 (`74f9963`) / TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409) |
| `NEXUS-DEBT-010` | RESOLVED | Cerrada en TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409), no más tarde de F3.4 | Auditoría post-RDD #403/#404; issue #405; cierre #411 | El validator browser implementa un subset JSON Schema que cubre todos los keywords actuales, pero no falla cerrado si un schema futuro introduce un keyword de validación no soportado; `String.length` tampoco reproduce plenamente longitud Unicode JSON Schema para caracteres astrales. | Gate build-time fail-closed de allowlist de keywords en `tools/home_validators_build.mjs` con `--schema-dir` y anotaciones reconocidas/eliminadas; longitudes Unicode en code points mediante `codePointLength` en los validators generados; nuevo `tools/nexus_home_validator_hardening_check.mjs` con 14/0 negativos plantados y `check:home-validators` en `verify:nexus`. Commit #411 `49deb61`; native review `APPROVED + burned` `review-b11ff9bef660e52d`. | #411 (`49deb61`) / TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409) |
| `NEXUS-DEBT-011` | RESOLVED | Cerrada en TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409), lo que habilita D009 | Post-merge fresh-checkout verification de PR #404; issue #407; cierre #410 | Bajo `.gitattributes` (`*.js`/`*.json eol=crlf`), los artefactos committed salen CRLF y los builders Home generan LF: CASO 7/8 de `nexus_home_check` fallan byte-identical en fresh checkout aunque cada par sea `normalized_equal=true`. No se observó fallo runtime: nav 11/11 y `verify:nexus` PASS. | `.gitattributes` fija LF canónico con alcance acotado a `modules/home/home-schema-validators.generated.js`, `data/platform/home/*.json` y `tools/fixtures/home/*.json`; detección byte-strict de drift preservada; fresh checkout Home 11/11 + navegación 11/11 + `verify:nexus` PASS. Commit #410 `74f9963`. | #410 (`74f9963`) / TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409) |
| `NEXUS-DEBT-012` | OPEN | Antes de usar el release manifest Home como evidencia de release operacional/piloto; puede entrar en el siguiente train Home si toca release tooling | Promotion Review PR #415 N1 (nonblocking) | `home_release_build.mjs` declara `gates.browser.suite = tools/nexus_home_f33_browser_check.mjs` con requisito `PASS on the release artifact`, pero esa suite cualifica el sitio completo; la evidencia browser real del artefacto aislado es `tools/nexus_home_release_browser_check.mjs` 5/0 y no queda apuntada por el manifest. El checker estructural además exige el literal F3.3 actual. | Alinear el evidence pointer del manifest con la suite que realmente cualifica el artefacto (o modelar ambas de forma explícita), actualizar checker/doc y demostrar que el release manifest reproduce la evidencia correcta sin rebajar gates. | PR #415 Promotion Review / pendiente de WO |
| `NEXUS-DEBT-013` | OPEN | Antes de ampliar schemas Home a formas no ejercitadas; no más tarde de una frontera piloto que dependa del validator browser | Promotion Review PR #415 N3 (nonblocking) | El gate D010 es por nombre de keyword: `items` está allowlisted y el walker acepta su forma array, pero el intérprete generado solo implementa `items` como schema único; una forma array podría superar ese gate y quedar no-op. Los cuatro schemas actuales no usan esa forma y Ajv 2020 strict falla antes, por lo que no se observó fail-open actual. | Hacer el gate sensible a la forma y rechazar determinísticamente `items` array (o implementar semántica explícita si alguna autoridad futura la adopta), con negativo plantado y rebuild reproducible; no ampliar schemas por oportunidad. | PR #415 Promotion Review / pendiente de WO |

## Reglas de uso

- Todo finding material aceptado de Promotion Review/RDD que deba sobrevivir al cierre de una PR entra aquí o en el registro de módulo correspondiente.
- El issue origen conserva el detalle; este registro mantiene la vista operativa y estable.
- `OPEN` no equivale a prioridad inmediata. El timing se decide por riesgo y dependencia real.
- `BEFORE_PILOT`, `BEFORE_F3` u otra condición de fase significa que no puede olvidarse antes de cruzar esa frontera.
- Una deuda solo pasa a `RESOLVED` con evidencia publicada; si se mueve a otro issue/WO se marca `TRANSFERRED` y se enlaza.
- No mezclar deudas heterogéneas en una WO solo para vaciar el registro.

## Próximo consumo

`NEXUS-DEBT-001`, `NEXUS-DEBT-002` y `NEXUS-DEBT-006` están RESOLVED dentro de TRAIN-NEXUS-FOUNDATION-02 (#399), publicado por PR #400. `NEXUS-DEBT-008`, `NEXUS-DEBT-009`, `NEXUS-DEBT-010` y `NEXUS-DEBT-011` están RESOLVED y publicados dentro de TRAIN-NEXUS-HOME-QUALIFICATION-03 (#409) mediante PR #415 (candidate `9004b443...` → merge `6e6413c4...`). Permanecen abiertos `NEXUS-DEBT-004`, los subhallazgos restantes de `NEXUS-DEBT-005`, `NEXUS-DEBT-007` y los nuevos `NEXUS-DEBT-012/013` preservados por la Promotion Review de #415. D012 debe cerrarse antes de elevar el release manifest Home a evidencia operacional/piloto; D013 antes de ampliar schemas a formas no ejercitadas o, como máximo, antes de una frontera piloto dependiente del validator browser. D7 pertenece al hardening de export Reuma y D4 se decide cuando el release map sea frontera consumida. Ninguna deuda abierta acredita un fallo clínico ni autoriza implementación por sí sola. Cualquier implementación futura requiere WO explícita.
